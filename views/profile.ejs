<!doctype html>
<html>
<head>
    <title>Barista App</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <script src="https://kit.fontawesome.com/ed20920919.js"></script>
    <link rel="stylesheet" href="style.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center">
        <h1><i class="fas fa-archway"></i> Barista Order Screen</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> <%= user.local.firstName%> </h3>

                <% if (user.local.email) { %>
                    <p>
                        <img src="img/coffee.jpg" alt="">
                        <strong>Name</strong>: <span id="completingBarista"><%= user.local.firstName%></span><br>
                        <strong>Last Name</strong>: <%= user.local.lastName %><br>
                        <strong>email</strong>: <%= user.local.email %><br>

                    </p>
                    <!-- <strong>password</strong>: <%= user.local.password %> -->

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div>
        <div class="col-sm-6">
          <h3>Pending Orders</h3>
          <ul class="messages">
          <% for(var i=0; i<messages.length; i++) {%>
            <% if( messages[i].completed === false) {%>
            <li class="message">
              <span>Customer :</span>
              <span><%= messages[i].customerName %></span>
              <span>Order :</span>
              <span><%= messages[i].order %></span>
              <span><i class="fas fa-pencil-alt" aria-hidden="true"></i></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
            <% } %>
          <% } %>
          </ul>

          <h3>Completed Orders</h3>
          <ul class="messages">
          <% for(var i=0; i<messages.length; i++) {%>
            <% if( messages[i].completed === true) {%>
            <li class="message">
              <span>Customer :</span>
              <span><%= messages[i].customerName %></span>
              <span>Order :</span>
              <span><%= messages[i].order %></span>
              <span>Completed by:</span>
              <span><%= messages[i].completedBy %></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
            <% } %>
          <% } %>
          </ul>



</div>

<script src="main.js"></script>

</body>
</html>
